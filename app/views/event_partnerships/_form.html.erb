<%= form_with(model: event_partnership) do |form| %>
  <% if event_partnership.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(event_partnership.errors.count, "error") %> prohibited this event_partnership from being saved:</h2>

      <ul>
        <% event_partnership.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :event_id, style: "display: block" %>
    <%= form.text_field :event_id %>
  </div>

  <div>
    <%= form.label :partnership_id, style: "display: block" %>
    <%= form.text_field :partnership_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
